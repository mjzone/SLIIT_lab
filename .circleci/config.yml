version: 2
jobs:
  build:
    working_directory: ~/tmp
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run:
          name: npm Installation
          command: npm install
      - run:
          name: Run Tests
          command: npm test
   